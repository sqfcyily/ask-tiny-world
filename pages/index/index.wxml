<view class="container">
  <!-- 背景，使用用户提供的第二张图片 -->
  <image class="bg-layer" src="/images/bg.png" mode="aspectFill"></image>

  <!-- 聊天区域，整体不滚动 -->
  <view class="chat-area">
    <!-- AI气泡框（参考图1样式） -->
    <view class="ai-bubble-container" wx:if="{{latestAIMessage}}">
      <!-- 气泡主体，使用 scroll-view 以便内容过多时内部滚动 -->
      <scroll-view class="ai-bubble" scroll-y="true" scroll-into-view="{{scrollToMessage}}">
        <view id="bubble-content" class="bubble-content">
          <rich-text class="msg-text" nodes="{{latestAIMessage.formattedContent}}"></rich-text>
          <view class="loading" wx:if="{{latestAIMessage.loading}}">
            <view class="dot"></view><view class="dot"></view><view class="dot"></view>
          </view>
        </view>
      </scroll-view>
      <!-- 气泡的小尾巴 -->
      <view class="bubble-tail"></view>
    </view>
  </view>

  <!-- 信封收集区 (左边草地)，代表历史记录 -->
  <!-- 作为一个整体进行光晕闪动动画 -->
  <view class="envelopes-container {{envelopes.length > 0 ? 'container-glow' : ''}}" bindtap="toggleHistory">
    <view 
      wx:for="{{envelopes}}" 
      wx:key="id"
      class="envelope-wrapper"
      style="transform: translate({{item.offsetX}}px, {{item.offsetY}}px) rotate({{item.rotation}}deg); z-index: {{index}};"
    >
      <view class="envelope-item">✉️</view>
    </view>
  </view>

  <!-- 底部操作区 -->
  <view class="input-area">
    <!-- 语音按钮 -->
    <view class="btn-container voice-btn-container" bindtouchstart="startRecord" bindtouchend="stopRecord">
      <view class="text-bubble-hint voice-hint">{{isRecording ? '松开发送' : '说话提问'}}</view>
      <!-- <image class="btn-icon voice-icon {{isRecording ? 'recording' : ''}}" src="/images/voice-btn.png" mode="aspectFit"></image> -->
      <view class="btn-icon {{isRecording ? 'recording' : ''}}">🌻</view>
    </view>

    <!-- 文本按钮 -->
    <view class="btn-container text-btn-container" bindtap="toggleTextInput">
      <!-- 信箱图标上面有一个小气泡 -->
      <view class="text-bubble-hint text-hint">输入问题</view>
      <!-- <image class="btn-icon text-icon floating" src="/images/text-btn.png" mode="aspectFit"></image> -->
      <view class="btn-icon text-icon">📮</view>
    </view>
  </view>

  <!-- 文本输入弹窗 -->
  <view class="text-input-mask" wx:if="{{showTextInput}}" bindtap="toggleTextInput">
    <view class="text-input-box" catchtap="preventD">
      <input 
        class="input-element" 
        placeholder="想问什么呀？" 
        value="{{inputText}}" 
        bindinput="onInput" 
        confirm-type="send" 
        bindconfirm="sendText" 
        focus="{{showTextInput}}" 
      />
      <view class="send-btn" bindtap="sendText">发送</view>
    </view>
  </view>

  <!-- 历史对话弹窗 -->
  <view class="history-mask" wx:if="{{showHistory}}" bindtap="toggleHistory">
    <view class="history-box" catchtap="preventD">
      <view class="history-header">
        <text class="history-title">🌟 我们的回忆信箱 🌟</text>
        <view class="history-close" bindtap="toggleHistory">✖</view>
      </view>
      <scroll-view class="history-scroll" scroll-y="true">
        <view class="history-list">
          <view class="history-item" wx:for="{{chatHistory}}" wx:key="id">
            <view class="history-q-box">
              <view class="history-bubble user-bubble">
                <text>{{item.question}}</text>
              </view>
              <view class="history-avatar user-avatar">👦</view>
            </view>
            <view class="history-a-box">
              <view class="history-avatar ai-avatar">{{item.aiAvatar || '🤖'}}</view>
              <view class="history-bubble ai-history-bubble">
                <rich-text class="msg-text" nodes="{{item.answer}}"></rich-text>
              </view>
            </view>
          </view>
        </view>
      </scroll-view>
    </view>
  </view>
</view>